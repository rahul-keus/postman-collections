meta {
  name: Publish zigbee coordinator
  type: http
  seq: 1
}

post {
  url: {{BASE_URL}}/v1/zigbee_coordinator_store/publish_zigbee_coordinator
  body: json
  auth: inherit
}

body:json {
  {
      "branch": "dev_gateway_restructure",
      "os" : "raspbian",
      "platform:": "pi5"
  }
  
}

settings {
  encodeUrl: true
}
