meta {
  name: gateway get plugin template
  type: http
  seq: 4
}

post {
  url: {{BASE_URL}}/v1/plugin_template_store/gateway.get_plugin_template
  body: json
  auth: inherit
}

body:json {
  {
      "templateId" : "hello_world_template:1.0.0"
  }
  
}

settings {
  encodeUrl: true
}
