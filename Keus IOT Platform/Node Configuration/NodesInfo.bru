meta {
  name: NodesInfo
  type: http
  seq: 6
}

post {
  url: http://{{NodeHost}}:{{NodePort}}/keus/~node/list
  body: json
  auth: inherit
}

body:json {
  {
      "services": [{
          "id": "PLUGIN_CORE",
          "serviceType": "INTRA_PROCESS",
          "serviceFile": "/services/plugin-core/index"
      }]
  }
}

settings {
  encodeUrl: true
}
