meta {
  name: NodeServicesInfo
  type: http
  seq: 5
}

post {
  url: http://{{NodeHost}}:{{NodePort}}/keus/~node/services
  body: json
  auth: inherit
}

body:json {
  {
      "services": [{
          "id": "PLUGIN_CORE",
          "serviceType": "INTRA_PROCESS",
          "serviceFile": "/services/plugin-core/index"
      }]
  }
}

settings {
  encodeUrl: true
}
